import { Stack } from 'expo-router'
import { StyleSheet } from 'react-native'

const GiftSetLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown : false
      }}
    />
  )
}

export default GiftSetLayout

const styles = StyleSheet.create({})